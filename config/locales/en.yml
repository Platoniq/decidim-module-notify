---
en:
  activemodel:
    attributes:
      note:
        code: Author
        body: Statement
  decidim:
    components:
      notify:
        name: Notify
        settings:
          global:
            announcement: Announcement
          step:
            announcement: Announcement
    notify:
      admin:
        conversations:
          success: "Configuration saved successfully"
          error: "Error saving configuration [%{message}]"
          index:
            title: Participants for the conversation
            save: Save changes
        actions:
          new_group: New group
      conversations:
        statement: Write the statement...
        author: Author, use the number or name
        error: "Error saving the note [%{message}]"
        index:
          conversations: Conversations
          send: Send

